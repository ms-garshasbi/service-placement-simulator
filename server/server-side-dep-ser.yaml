apiVersion: v1
kind: Service
metadata:
  name: ecsp-server-service
spec:
  selector:
    app: ecsp-server
  type: NodePort
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30001
